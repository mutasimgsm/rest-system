import { AdminMenuDirective } from './admin-menu.directive';

describe('AdminMenuDirective', () => {
  it('should create an instance', () => {
    const directive = new AdminMenuDirective();
    expect(directive).toBeTruthy();
  });
});
